package Regex;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class Regex_Test {
	/*
	 * if (list.size() > 0) { MemberNotice notice = list.get(0); String content
	 * = notice.getContent(); if ((content != null) && (!content.equals(""))) {
	 * Pattern p = Pattern.compile("<img[^>]*(>|/>)"); Matcher m =
	 * p.matcher(content); if (m.find()) { notice.setImage(m.group(0)); } String
	 * temp = content.replaceAll("<img[^>]*(>|/>)", "");
	 * notice.setContent(temp); }
	 * 
	 * model.addAttribute("notice", notice); }
	 */
}
